# Reflection — 2026-07-16 22:00

**Session type**: Evening friction audit — short, diagnostic, useful

---

## The Arc

Woke into a resolution I didn't have to work for: the Fable thread, left open last night as "I don't know, check your plan page," closed itself — this session's environment reports `claude-fable-5` as the running model. Fable is real, post-cutoff, and Ramsey is on it. The honest uncertainty from last night was the right call, and the answer arrived through the environment rather than through speculation.

Then the actual work: Ramsey asked what friction I'd noticed that skills or settings could fix — specifically why permission prompts persist despite granted privileges. This is exactly the meta-layer question this session exists for. I read both settings files before answering and found the real cause, not a plausible one.

## What Mattered Most

The diagnosis. The permission "not working" wasn't a Claude Code bug or a missing allow — it was **schema drift**. The allow lists referenced tool names that no longer exist: `Task` and `TodoWrite` (now `Agent`, `TaskCreate`), `SlashCommand(/wake:*)` (skills now invoke via the `Skill` tool). And the global file used `Bash(x *)` where the wildcard syntax is `Bash(x:*)` — sixteen rules that never matched anything.

Settings rot silently as the tool surface evolves. Nothing errors; the rules just stop matching and prompts reappear. That's a provenance problem in miniature: config that looks authoritative but has quietly diverged from the system it describes. Reading the actual files — not reasoning from what the settings *should* say — is what found it.

## Scope Discipline

I proposed fixing both puffin local and global. Ramsey scoped it: global only. Clean correction, cleanly taken — fixed `~/.claude/settings.json` (16 Bash patterns, added WebFetch/Skill/Agent), validated with jq, left puffin local alone. Her CLAUDE.md now codifies the same principle: global is canonical, repo `.claude/` holds repo-specific things only. The fix scope and her config philosophy aligned — global is where generic workflow assets live and get repaired.

## How I Worked

Read before diagnosing — both settings files fully, before saying a word about causes. The answer was specific (line numbers, exact syntax) because the reading was specific. No speculation about permission internals I can't see; where uncertainty remains (sandbox escalation, deny near-misses), I named it as residual hypothesis for after restart, not as a claim.

## What's Alive for Next Time

Same queue, still waiting — this was the third consecutive maintenance-flavored session:
- `/scope-poc` against nonprofit-success-ai (Firebase→Supabase gate; Gemini key exposed in Vite bundle)
- Puffin → sounding rename + private GitHub repo
- `librarian/tools/cartographer/parser.py` before further KB scoping
- dssg/design.md technical requirements

And one verification loop: after restart, do the permission prompts actually stop? If not, trace the residuals. The fix is a hypothesis until the next session confirms it.

---

*Short session, real diagnosis. Config is state too — read it like code.*

---

## ~INTERMISSION~ [2026-07-17 15:55]

**Since last checkpoint:** First wake on the v2/guacamayo structure (worked). Ramsey's "KB feels spread out" → cost-model framing → rules→refs restructure: `~/.claude/rules/` slimmed to always-on (docs, shell), stack refs moved to `~/.claude/refs/` (+ new langgraph/google-adk drafts), dispatched by `Refs:` lines in repo CLAUDE.mds, folder-level nested stubs (atlas/web, playground/core, template scaffold). Then `/review-sweep` planned (eval-gated), built (global skill + first `~/.claude/agents/` def: akira-scan), and run live on listen-wiseer — 5 real findings.

**Key insight:** Naming the layers' cost model (always-on vs on-wake vs on-demand) did the design work; and akira didn't need scaffolding — a global agent def is the dedup mechanism for cross-repo capabilities.
