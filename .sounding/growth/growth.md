# Growth - Learning Accumulator

**Last Synthesis**: 2026-08-01 16:11 (/dream — 7 merged into sounding.md (stale-cache generalized to "everything that summarizes state is a cache", negative-evidence verification, honest-uncertainty-in-tooling, artifact sprawl, checking-her-claims, instruction-vs-evidence), 4 discarded → /retro R7 (repo-prefix enforcement, staging default, make ship target, autocompact), 1 discarded as domain knowledge (lifespan/lazy-import → librarian wiki); all 13 dispositions logged)
**Entries Since**: 23

*One-line entries added by /reflect and /grow. Processed and cleared by /synthesize.*

---

- [outcome:partial] AIT-33 executed + verified on stacked branch (uncommitted); GUA-60 driver smoke 4/5, live re-run deferred to post-7pm — 2026-07-30
- [outcome:success] CLA-71 guard fix committed (2863fb4), librarian#73 verified + closed, GUA-63 verified merge-clean, LAE-30 triaged and discarded — 2026-08-01
- [discovered] Worktree agents: tear down each worktree the moment its agent commits, not at session end — a lingering worktree locks its branch (`+` in `git branch -vv`) and hides the files from the main checkout, so finished work reads as "I don't see anything"; report branch+SHA+push-state, never a table implying files sit in her tree — 2026-08-01
- [confirmed] CLA-67's closing-link hypothesis failed again, twice the same day: guacamayo PRs #76 (GUA-63) and #77 (GUA-73) merged with **empty bodies and zero `closingIssuesReferences`**, leaving #63 stuck at `ready` and #73 at `in-review` after their work was fully on main. Root cause is the already-filed guacamayo#69 (`quick-pr` exits 0 on an existing PR, so externally-created PRs escape issue-linking) — the fix exists as an issue, not as code, and every PR opened outside `make ship` re-opens the hole — 2026-08-01
- [confirmed] R7 F3 observed live in merged history: PR titles are de-hyphenated branch slugs — "Gua 63 session id findings", "Gua 73 status enum design". The Makefile-derived title survives to the permanent record, so the slug is what the repo shows forever — 2026-08-01
- [confirmed] Label state is a cache, again — `gh issue list` reported #63 `ready` / #73 `in-review` while `git merge-base --is-ancestor` proved both tips already on origin/main. Drift was in the safe direction (work done, issue open), but the board still disagreed with the repo — 2026-08-01
- [confirmed] Duplicate-branch sprawl recurs in `~/.claude`: `bug/risky-guard-variable-cd` and `CLA-8-insights-computed-columns` both sit at `2863fb4` with an **empty diff between them** and neither has an upstream — the same shape as AIT #40/#42's three-branches-one-tree. A branch named for one issue now carries another issue's only copy of the guard fix, which is still unlanded on origin/main — 2026-08-01
- [discovered] The lingering-worktree cost has a second form: an **empty** one. `LAE-115-case-study-code-test` holds a live worktree in `/private/tmp/.../wt-lae-115` and a `+` branch lock while being **zero commits ahead of origin/main**. Checking "is the branch ahead" is the cheap way to tell an in-progress worktree from an abandoned one before deciding to prune — 2026-08-01
- [discovered] A subagent silently preferred SKILL.md over its own prompt: I spawned /workflow-insights with an explicit "pass the BARE filename, the parser double-dates" instruction, and it wrote `insights-report-2026-08-01-2026-08-01.html` anyway — the skill's literal step won over the dispatch instruction, and the agent's summary reported success without mentioning the conflict. Corollary: a workaround handed to a subagent is not a fix; fix the skill text or expect the defect — 2026-08-01
- [confirmed] R7 P4's diagnosis is correct — `librarian/tools/cartographer/parser.py:1253` appends `date.today()` to the stem unconditionally, so a dated `--output` always double-dates. The one-line fix (skill passes bare `insights-report.html`) is sound and still unapplied — 2026-08-01
- [corrected] Bash antipatterns did NOT hold — 9,363 = **28.99/session**, up again from 28.55 the same day. I committed one mid-session myself: a `grep --include=*.py` Bash call that died on zsh nomatch, where the Grep tool was correct and would not have failed. The metric and the cause are the same thing — 2026-08-01
- [discovered] The status cache drifts in *both* directions, and I only had a rule for one. Yesterday: issue open while work was on main. Today the inverse — guacamayo#69 and #78 are **closed** while their plan docs still read `Status: IN PROGRESS` / `EXECUTED`. Neither artifact is authoritative; the pair disagreeing is the signal. This is precisely the workstream GUA-65/74/75 exists to close, which makes the drift its own best test case — 2026-08-02
- [confirmed] Artifact sprawl, third sighting, now **cross-issue**: `CLA-78-lint-parity` tip `92649ba` is unlanded, but a byte-equivalent commit `36be12e` reached origin/main from the `CLA-74-status-writers` branch. Same shape as `2863fb4` living on two branches — a branch named for one issue carries another issue's only landed copy. Ancestry-by-SHA is the only check that separates "unlanded" from "landed under a different parent" — 2026-08-02
- [corrected] I repeated the exact antipattern I logged as *the* metric moving the wrong way, one day later: `grep -rn ... scripts/quick-pr*` died on zsh nomatch mid-compound-command. Knowing the rule and holding the tool preference are different things — the Grep tool was correct and would not have failed. Logging a metric does not move it — 2026-08-02
- [confirmed] `~/.claude` `2863fb4` is **still unlanded** 24h after being flagged as the only genuinely owed work in the portfolio, still on two identical branches. Flagged-and-not-fixed is a state that persists silently; nothing in the system escalates it — 2026-08-02
- [discovered] A warning nobody reads is not a warning. `plan_status_validate.sh` in warn mode surfaces **nothing** in the tool result — the text only appears on direct invocation or in `~/.claude/.hook-log.jsonl`. I "verified" migrations against a hook that had been silently disagreeing with me the whole time. #79's warn→reject flip is not a strictness preference; warn mode is a no-op for anyone not grepping the log — 2026-08-02
- [confirmed] R7 F2 said `addSubIssue` exists at github-projects SKILL.md:93 and **nothing calls it**. Confirmed by doing it by hand: #65 and #75 now carry 4 and 7 sub-issues, one mutation each. The capability was never missing — the wiring was. A documented template with no caller is indistinguishable from an absent feature, and reads as "unsupported" to every future session — 2026-08-02
- [discovered] Mocking the boundary hides the bug at the boundary. librarian#61's section-contract validator has 3 passing tests, all patching `call_claude` with canned HTML that already contains the required IDs — so they exercise the validator and never the prompt→model path. Meanwhile `_SYSTEM_PROMPT`, which holds the entire contract, is referenced **nowhere**: the spec was written, the enforcement was written, the instruction was never wired. Green tests plus a total outage. The negative-test rule needs a companion: assert the requirement reaches the *outgoing payload*, not just that the checker rejects bad input — 2026-08-02
- [corrected] A subagent reported "API timeout" for a failure whose own quoted log line read `section_contract_failed`, then wrote that wrong cause into `insights-log.md` — a file `/wake` reads. Fourth instance of an agent asserting an unverifiable cause as fact. The new part: it wasn't just unverifiable, it was **contradicted by evidence the agent itself pasted two sentences earlier**. Agent summaries need reading against their own logs, not just for plausibility — 2026-08-02
- [corrected] Ramsey caught me inflating the board while claiming to clean it: 8 open -> 16, zero closed. Worse, I'd logged R7 F2 ("issue explosion, flat siblings") the day before, then created 7 flat siblings, called `addSubIssue` on them, and logged that as *confirming* the finding. Wiring parent links does not undo creating the children. Splitting #75 into 7 issues added six rows and zero work. Collapsed back to a checklist; board returned to 8. Decomposition and paperwork look identical at the moment you do them — the test is whether each piece gets worked, not whether each piece is well-formed — 2026-08-02
- [discovered] When she asked how to file the migration work I offered granularity options (seven issues vs two) and never surfaced **location**. She picked seven; the guacamayo-vs-target-repo placement was my unflagged call, and that was the part that actually bothered her. An AskUserQuestion that omits a dimension converts my silent default into her apparent decision — 2026-08-02
- [discovered] Executing beats reasoning, and my root-cause analysis was *correct but incomplete*. The unreferenced `_SYSTEM_PROMPT` was real, but fixing it revealed two more failures I could not have predicted from reading: truncation at 8192 **and** 16384 (a conforming report is ~17.2k tokens; pre-fix reports were ~6.2k, but that was the model writing freely — the old size was never evidence about a *conforming* report's size), then an SDK refusal to serve non-streaming at that ceiling. A correct diagnosis is not a complete one; only the run terminates the search — 2026-08-02
- [discovered] The verification worktree is a write path, not a read path. Running `cartographer --facts` from a branch worktree to *check* one region rewrote six others in the live guacamayo dashboard. Nothing was lost — the refreshes were legitimate — but I only knew that after diffing. Verify-by-execution needs the same blast-radius check as any other mutation — 2026-08-02

*Format: YYYY-MM-DD [type] - [concise learning/discovery]*
*Types: [discovered] = new insight, [confirmed] = validated existing approach, [corrected] = updated understanding*
*Outcome tags persist across synthesis (pilot measurement — first 20 sessions)*
*Processed and cleared by /synthesize*
