# Tooling Ledger

Every change to the tooling layer (hooks, skills, rules, settings) gets a row: what
changed, the friction that motivated it, and whether the fix actually took. `/retro`
reads this first — `hypothesis` rows are its top queue. Statuses: `hypothesis` (change
made, effect unproven) → `verified (evidence)` or `failed (evidence)`. Keep under ~1
screen: compress verified rows into monthly rollups.

| Date | Change | Motivating friction | Status |
|---|---|---|---|
| 2026-07 (rollup) | Settings Bash patterns (colon-form); /retro + ledger established; cartographer keyless + relinker anchor; /config-audit created; v2→v3 single-writer lifecycle proven (2 syntheses, capture-only confirmed); doc-artifact collapse (single dated plan doc, no SESSION.md); ledger relocated to guacamayo; /retro Step 5 apply-gated-on-approval (first end-to-end run 2026-07-18); akira global port (3 modes, eval-verified) | 11 items verified Jul 2026 — see git/session history for per-change detail | verified |
| 2026-07-17 | sanyi promoted to global reservoir; SANYI.md contracts in playground/librarian/atlas; contract check added to review-pr/code-review skills | Skill copies drifting across repos; architectural decay (hardcoded tunables, invariants made configurable) invisible to diff-scoped review | hypothesis — verify: next reviews in contract repos surface contract findings; no duplicate-skill drift recurs |
| 2026-07-17 | sync-global-skills.sh prints per-skill diff detail + `--dry-run` flag (also fixed: diff exit code tripped pipefail and killed the summary) | Sync reported changes without direction/content (retro F1) | hypothesis — verify: next unexplained sync is diagnosable from output alone |
| 2026-07-17 | rules/style.md zsh-gotchas block; rules/docs.md "pointers, not copies" line (growth-entry graduation) | 4+ aborted compound commands per session (F4); handover copies drifted across 3 sessions (F5) | hypothesis — verify: 0 zsh aborts next long session; no stale-copy incident in next 3 handovers (pointers-not-copies graduated to sounding.md 2026-07-18) |
| 2026-07-17 | Skill dedupe: puffin `research` → global `parallel-research` (renamed, path-genericized); puffin `skill-writer` deleted, its gotchas/reference docs merged into global `skill-creator/references/`; placement rule codified in `~/.claude/rules/docs.md` | Same-name skills in repo + global = ambiguous dispatch (feedback-loop plan Phase 3b) | hypothesis — verify: /parallel-research and /skill-creator trigger correctly from puffin sessions |
| 2026-07-17 | Factual record ceded to librarian: 6 chat logs + index → raw/sessions/puffin-*, 3 genesis research notes → raw/playground-docs/, puffin added to repos.txt; puffin chats/ + chat-logs.md deleted | Quadruplicated session records; librarian already held independent record (research A3/C3) | hypothesis — verify: librarian session ingests without orphan-raw blockers; no missing-record incident in 3 sessions |
| 2026-07-17 | ~/.claude/commands/ retired (deleted; wrappers absorbed into skills): research/plan/execute skills renamed to short names, insights converted to skill, clean_memory absorbed into config-audit Checks 3–4, start/end_session dropped; cross-refs fixed (global CLAUDE.md, code-debug, plan-refactor, parallel-research, listen-wiseer) | Legacy dual-dispatch layer with near-zero usage and duplicated routing (research B3) | hypothesis — verify: /research /plan /execute /insights dispatch correctly for 3 sessions; no stale-reference errors |
| 2026-07-17 | puffin SessionStart wake-nudge hook + settings.local.json stale allows fixed (Task→Agent, SlashCommand→Skill) | /wake skipped on fresh sessions (idea floated 2026-07-16, never built); stale allows diagnosed 2026-07-16, left pending | hypothesis — verify: nudge appears and /wake runs in next 2 fresh puffin sessions |
| 2026-07-17 | Phase protocols (research/plan/execute/code-review) made target-repo aware: `repo:<name>` token resolves paths/git/tests against any workspace repo; artifacts always land in the target repo's plans/. ai-project-template's discovery stack deliberately NOT globalized (template stays self-contained for non-Ramsey setups) | Meta-session scoping from guacamayo would drop artifacts in the wrong repo (worked only by cwd accident); discovery-before-build had no codified cross-repo path | hypothesis — verify: first `repo:`-scoped run lands its doc in the target repo's .claude/docs/plans/ |
| 2026-07-17 | Rules→refs split: `~/.claude/rules/` slimmed to always-on only (docs.md, new shell.md); stack conventions moved to `~/.claude/refs/` (python, typescript from style.md split; logging/ml/adk-vercel moved verbatim; langgraph + google-adk drafted new — Ramsey review). Dispatch = `Refs:` line in repo CLAUDE.md (added: atlas, listen-wiseer, playground, librarian, project-mgmt-ai; template CLAUDE.md.jinja gets a var-conditional Refs block); global CLAUDE.md Tooling section replaced with the refs convention | Five stack-specific rule files auto-injected into every session regardless of repo/stack — token tax + irrelevant constraints in non-code sessions | hypothesis — verify: next Python/TS sessions read the right refs via repo `Refs:` line (conventions still applied); next template render emits correct Refs block |
| 2026-07-17 | `/review-sweep` global skill + `~/.claude/agents/akira-scan.md` (first global agent def): standing repo/workspace review — repo-Makefile mechanical checks, SANYI contract, parallel akira-scan quality scan, doc-drift flags; code-review gains cross-pointer. Akira decision: global agent replaces scaffolding as the cross-repo mechanism (scaffold stays a template feature). Plan + acceptance criteria: `guacamayo/.claude/docs/plans/2026-07-17-review-sweep.md` (first eval-gated skill change) | Review capability was per-repo (akira scaffold-only, blocked on hardcoded scan roots) with no plan-less entry point; quality/contract/docs checks uncomposed | hypothesis — first run same session (listen-wiseer fast): pipeline worked end-to-end, 5 real findings incl. TodoWrite settings-rot, report landed in target repo, no files edited; agent-type dispatch failed mid-session (defs load at startup — used Explore fallback). Still verify fresh-session akira-scan dispatch + seeded-bug catch + SANYI mapping + sweep dirty-repo routing |
| 2026-07-17 | Review ladder: /review-sweep gains `level:1|2|3` (+`headless` never-block rule, Needs-input report section; hard rule: 3 levels, no sub-flags); workspace Makefile (existing precommit/test/pull kept) gains help + review-fast/review/review-deep targets that PRINT the in-session command (decided: no headless auto-run); docs: global CLAUDE.md ladder para, guacamayo README ladder section (Ramsey-directed), template README pointer. Plan: `2026-07-17-workspace-review-ladder.md` (eval-gated #2) | Review had one cost setting; no terminal entry point; sweep/commit ordering undocumented; sanyi audit cost unbounded across repos | hypothesis — verify: L1 spawns zero agents+skips tests; L3 refuses sweep; make targets print correct commands (verified same-session for print forms); L1-vs-L2 token comparison on same diff |
| 2026-07-17 | Model pairing codified: `~/.claude/refs/models.md` (all global skills + lifecycle skills + agent spawns → haiku/sonnet/opus tiers; "pay for judgment, not for reading"); pointer para in global CLAUDE.md; review-sweep step 4 pins haiku + verify-before-report explicitly | Model choice was implicit/per-whim — fan-out sometimes ran at session (opus) prices; no documented guidance for which session tier to invoke judgment-dense skills in | hypothesis — verify: agent spawns in next sessions specify haiku where the ref says so; no opus-session sweep for routine L1/L2; ref consulted (not duplicated) when new skills/agents are created |
| 2026-07-17 | Session hygiene codified (global CLAUDE.md + models.md line): one work item per session, plan-doc as continuity, /execute in fresh sonnet session, meta sessions dispatch via 3-line spawn prompts into IDE sessions | /insights 7d: 66% of usage at >150k context, 12% on /execute in bloated sessions, terminal-spawned work invisible to Ramsey (wants IDE) | hypothesis — verify via next /insights: >150k share <~40% (progress: 66%→58% as of 2026-07-18), /execute share down; spawn-prompt convention actually used (else escalate to mechanism) |
| 2026-07-17 | librarian retrieval telemetry: all 5 MCP wiki tools append JSON lines to logs/retrieval.jsonl (exception-swallowing helper, gitignored); tombstone pages excluded from search index at build time | Wiki retrieval unmeasured — no evidence base for compaction decisions (knowledge-compaction F4) | hypothesis — verify: retrieval.jsonl accumulating rows within a week of retrieval-first usage |
| 2026-07-17 | /compact-wiki skill created (librarian-local): evidence-driven merge/retire/compress with dry-run gate; CLAUDE.md gains Compaction Protocol, tombstone format, ingest accumulator ceiling, stale-lint→compaction consumer | Wiki compiled but never recompiled; aging primitives (supersedes, stale flags) had no consumer (knowledge-compaction F1/F2) | hypothesis — verify: first manual dry-run produces evidence-cited report Ramsey can approve without re-deriving candidates |
| 2026-07-17 | `~/.claude/refs/repo-security-setup.md` runbook (Dependabot enable + auto-merge workflow + alert triage + branch protection, baseline configs embedded; broken `-f null` protection command fixed to `--input` JSON) + pointer line in global CLAUDE.md Tooling section; automated same session as `~/.claude/scripts/repo-security-setup.sh` (ecosystem auto-detect, stages-never-commits, `--dismiss-path`/`--dismiss-all` flags) + `make secure REPO=x` workspace target | Per-repo GitHub security setup lived in one chat transcript; learn-ai-engineering had ~1k undismissed alerts and the referenced baseline files didn't exist anywhere | hypothesis — verify: next repo security setup runs from the ref without re-deriving commands; learn-ai-engineering alert count drops after scoped bulk-dismiss |
| 2026-07-17 | `.claude/docs/` git-ignored everywhere (Ramsey's call, against session recommendation): `~/.gitignore_global` + core.excludesfile; untracked via `git rm --cached` in 6 repos (staged, Ramsey commits); playground `!.claude/docs/` negation removed; rules/docs.md records local-only status + no-bulk-delete warning | Inconsistent tracking across repos (librarian ignored, learn-ai-engineering inert ignore line, playground partial); plan-doc noise in repos | hypothesis — RISK: ledger/state/plans now have no git history (the exact loss mode from the `~/.claude/docs/` deletion today); verify no lost-doc incident in 2 weeks, else revisit |
| 2026-07-18 | `~/.claude/hooks/memory_route_guard.sh` (new PreToolUse Write\|Edit\|MultiEdit hook) blocks writes to the global auto-memory dir and redirects identity/synthesis facts to guacamayo; MEMORY.md index stays writable (pointers only). Wired into settings.json alongside secrets_scan. Also dropped a stale MEMORY.md pointer (project_state_in_guacamayo.md — file never existed) | Global memory protocol wrote identity/self-identification facts to `~/.claude/projects/.../memory/` silently; Ramsey wants those in guacamayo (git-tracked, phone-synced, feeds /synthesize) and gated behind approval | hypothesis — verify: next auto-memory write attempt is blocked + redirected; no identity fact lands in global memory over 3 sessions |
| 2026-07-18 | shell.md gains deletion-safety rule (`git rm --cached` over `rm` for bulk ops; check `git ls-files` before bulk delete) | Unrecoverable ledger loss from rm of git-ignored dir; principle lived only in growth.md (retro graduation) | hypothesis — verify: no destructive-rm-on-tracked-files incident in 3 sessions |
